import InfoData from '..';

const InfoDataList = () => {
  return (
    <div className="flex">
      <InfoData version={0} />
      <InfoData version={1} />
    </div>
  );
};

export default InfoDataList;
